{
  "name": "deface-ios",
  "private": true,
  "version": "1.0.0",
  "description": "iOS wrapper for the DeFace Privacy PWA (Capacitor + WKWebView).",
  "scripts": {
    "www": "bash sync-www.sh",
    "sync": "bash sync-www.sh && cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
